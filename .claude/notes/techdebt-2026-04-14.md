## Tech Debt Report - 2026-04-14

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None in the touched long-session parity surface | n/a | No new critical debt found in the audited runtime/editor files | Keep the focused regression suite in place so replay/read-state drift is caught early |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Replay-state normalization is concentrated in two large helpers | `runtime/src/gateway/daemon-session-state.ts:305`, `runtime/src/gateway/daemon-session-state.ts:491` | The replay snapshot builder and coercion/migration path now own a lot of shape logic, which raises review and migration risk | Split snapshot extraction, replay-tail rendering, and legacy migration/coercion into smaller pure helpers with dedicated tests |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Shell write policy repeats operand-walking patterns across command families | `runtime/src/llm/shell-write-policy.ts:105`, `runtime/src/llm/shell-write-policy.ts:208`, `runtime/src/llm/shell-write-policy.ts:272` | New shell-write cases are more likely to drift because path/operand parsing is spread across several helpers | Factor shared operand scanning primitives for direct commands and wrapper commands |
| Session compaction now stores both artifact state and records in session metadata until replay persistence runs | `runtime/src/gateway/session.ts:566`, `runtime/src/gateway/session.ts:608` | This is correct for durability, but it keeps more structured state resident in memory and increases coupling between compaction and replay persistence | Consider an explicit session-compaction persistence abstraction so metadata only carries the minimum resume handle |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Replay snapshot field extraction and replay-state coercion mirror many of the same fields | `runtime/src/gateway/daemon-session-state.ts:305-386`, `runtime/src/gateway/daemon-session-state.ts:491-733` | ~325 | Shared field-schema helpers for shell/workflow/runtime-contract/review/verification/task/fork fields |
| Shell operand parsing branches share repeated `--` / flag-skipping logic | `runtime/src/llm/shell-write-policy.ts:105-131`, `runtime/src/llm/shell-write-policy.ts:178-191`, `runtime/src/llm/shell-write-policy.ts:216-240` | ~65 | One reusable operand iterator with mode flags |

### Summary
- Total issues: 4
- Estimated cleanup: 3 files
- Recommended priority: split the replay-state build/coercion path in `runtime/src/gateway/daemon-session-state.ts` before adding more resume metadata

## Tech Debt Report - 2026-04-14 (Failed-Tool/Search Parity)

### Critical (Fix Now)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| None remaining in the touched failed-tool/search surface after the follow-up fixes | n/a | The concrete regex-error and artifact snapshot publication issues found during the scan were fixed in-session | Keep the focused regression tests for `coding.ts` and `artifact-store.ts` in place |

### High (Fix This Sprint)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Search and inventory tools remain concentrated in one monolithic factory | `runtime/src/tools/system/coding.ts:638` | Search behavior is now closer to `../claude_code`, but the 1k+ line factory makes future search-policy changes harder to review and easier to drift | Split `system.grep`, `system.searchFiles`, `system.glob`, and repo inventory into dedicated helper builders/modules |
| Tool-loop recovery and protocol handling are still concentrated in one oversized executor loop | `runtime/src/llm/chat-executor-tool-loop.ts:1498` | The new failed-tool recovery path is correct, but it lands inside a very large control-flow surface with stop/protocol/compaction interactions | Extract recovery-streak policy and round-finalization branches into smaller pure helpers |
| Webchat turn orchestration is still too broad | `runtime/src/gateway/daemon-webchat-turn.ts:138` | Routing, tracing, persistence, hook dispatch, and transport emission are still coupled in one long function | Split request preparation, execution, and response persistence into dedicated helpers |
| Coding-tool preference policy is duplicated across prompt/profile/bundle surfaces | `runtime/src/gateway/system-prompt-builder.ts:188`, `runtime/src/gateway/shell-profile.ts:76`, `runtime/src/gateway/shell-agent-roles.ts:300` | Search/discovery guidance can drift because the same preferred tool set is described in three places | Centralize the preferred coding-tool set in one shared constant/helper consumed by prompts and shell bundles |
| Watch event taxonomy still has multiple manual mapping points | `runtime/src/watch/agenc-watch-surface-summary.mjs:1`, `runtime/src/watch/agenc-watch-surface-summary.mjs:252`, `runtime/src/watch/agenc-watch-surface-summary.mjs:275`, `runtime/src/watch/agenc-watch-surface-summary.mjs:740` | The `FAULT`→`ERROR` change touched several separate branches, which raises UI drift risk for the next event-kind change | Consolidate badge/status/meta derivation behind a single event-taxonomy helper |

### Medium (Backlog)
| Issue | Location | Impact | Suggested Fix |
|-------|----------|--------|---------------|
| Replay hydrate path still swallows thread-load failures as empty history | `runtime/src/gateway/daemon-session-state.ts:778` | Backend or replay corruption can look like a valid empty transcript, which makes restore bugs harder to diagnose | Log and classify `getThread()` failures instead of silently converting them to `[]` |
| Text-file cutoff is still a hard magic limit in coding search helpers | `runtime/src/tools/system/coding.ts:34` | Large-file search omissions remain silent once files exceed `512_000` bytes | Surface a skipped-large-file count/reason or move the limit into explicit tool config |
| Replay-state coercion still repeats several field-normalization passes inline | `runtime/src/gateway/daemon-session-state.ts:507` | Parser maintenance cost stays high because related coercions are repeated inline in one large helper | Break the coercion pipeline into reusable field-group helpers |

### Duplications Found
| Pattern | Locations | Lines | Refactor To |
|---------|-----------|-------|-------------|
| Preferred coding-tool policy is duplicated across prompt prose and shell bundle wiring | `runtime/src/gateway/system-prompt-builder.ts:188`, `runtime/src/gateway/shell-profile.ts:76`, `runtime/src/gateway/shell-agent-roles.ts:300-411` | ~40 | One shared “preferred coding tools” definition consumed by all three surfaces |
| Watch event-kind classification is duplicated across badge/status/meta/alert branches | `runtime/src/watch/agenc-watch-surface-summary.mjs:1-22`, `runtime/src/watch/agenc-watch-surface-summary.mjs:252-284`, `runtime/src/watch/agenc-watch-surface-summary.mjs:740-743` | ~60 | A single event taxonomy table with derived helpers |
| Failed-tool recovery policy is tracked both by streak state and by rescanning recent tool history | `runtime/src/llm/chat-executor-tool-loop.ts:265-315`, `runtime/src/llm/chat-executor-tool-loop.ts:2088-2096` | ~60 | One authoritative failed-tool streak helper that emits both count and summary |

### Summary
- Total issues: 8
- Estimated cleanup: 6 files
- Recommended priority: split the search/inventory tool factory and centralize coding-tool/watch-taxonomy policy before adding more discovery UX changes
